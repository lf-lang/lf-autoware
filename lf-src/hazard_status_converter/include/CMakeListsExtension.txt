unset(BUILD_TESTING)
set(LF_AUTOWARE_HOME $ENV{LF_AUTOWARE_HOME})
message(STATUS "Using ${LF_AUTOWARE_HOME} as LF Autoware home directory")

# Fix tinyxml2_vendor issue: pre-set TinyXML2 vars so vendor creates correct target
set(TinyXML2_FOUND TRUE)
set(TINYXML2_LIBRARY "/usr/lib/x86_64-linux-gnu/libtinyxml2.so")
set(TINYXML2_INCLUDE_DIR "/usr/include")


include_directories(
    ${LF_AUTOWARE_HOME}/lf-src/lf-include
    ${LF_AUTOWARE_HOME}/src/universe/autoware_universe/system/autoware_hazard_status_converter/src
)

find_package(autoware_hazard_status_converter REQUIRED)
find_package(autoware_adapi_v1_msgs REQUIRED)
find_package(autoware_system_msgs REQUIRED)
find_package(diagnostic_msgs REQUIRED)
find_package(rclcpp REQUIRED)
find_package(rclcpp_components REQUIRED)
find_package(tier4_system_msgs REQUIRED)

ament_target_dependencies(${LF_MAIN_TARGET} PUBLIC
    autoware_hazard_status_converter
    autoware_adapi_v1_msgs
    autoware_system_msgs
    diagnostic_msgs
    rclcpp
    rclcpp_components
    tier4_system_msgs
)

find_library(LIB_HAZARD_STATUS_CONVERTER autoware_hazard_status_converter
    PATHS ${autoware_hazard_status_converter_DIR}/../../../lib NO_DEFAULT_PATH)
if(LIB_HAZARD_STATUS_CONVERTER)
    target_link_libraries(${LF_MAIN_TARGET} PUBLIC ${LIB_HAZARD_STATUS_CONVERTER})
endif()
