unset(BUILD_TESTING)
set(LF_AUTOWARE_HOME $ENV{LF_AUTOWARE_HOME})
message(STATUS "Using ${LF_AUTOWARE_HOME} as LF Autoware home directory")

# Fix tinyxml2_vendor issue: pre-set TinyXML2 vars so vendor creates correct target
set(TinyXML2_FOUND TRUE)
set(TINYXML2_LIBRARY "/usr/lib/x86_64-linux-gnu/libtinyxml2.so")
set(TINYXML2_INCLUDE_DIR "/usr/include")


include_directories(
    ${LF_AUTOWARE_HOME}/lf-src/lf-include
)

find_package(autoware_predicted_path_checker REQUIRED)
find_package(autoware_motion_utils REQUIRED)
find_package(autoware_perception_msgs REQUIRED)
find_package(autoware_planning_msgs REQUIRED)
find_package(autoware_utils REQUIRED)
find_package(autoware_vehicle_info_utils REQUIRED)
find_package(diagnostic_updater REQUIRED)
find_package(geometry_msgs REQUIRED)
find_package(nav_msgs REQUIRED)
find_package(rclcpp REQUIRED)
find_package(rclcpp_components REQUIRED)
find_package(tf2 REQUIRED)
find_package(tf2_eigen REQUIRED)
find_package(tf2_geometry_msgs REQUIRED)
find_package(tf2_ros REQUIRED)
find_package(tier4_planning_msgs REQUIRED)

ament_target_dependencies(${LF_MAIN_TARGET} PUBLIC
    autoware_predicted_path_checker
    autoware_motion_utils
    autoware_perception_msgs
    autoware_planning_msgs
    autoware_utils
    autoware_vehicle_info_utils
    diagnostic_updater
    geometry_msgs
    nav_msgs
    rclcpp
    rclcpp_components
    tf2
    tf2_eigen
    tf2_geometry_msgs
    tf2_ros
    tier4_planning_msgs
)

find_library(LIB_PREDICTED_PATH_CHECKER autoware_predicted_path_checker
    PATHS ${autoware_predicted_path_checker_DIR}/../../../lib NO_DEFAULT_PATH)
if(LIB_PREDICTED_PATH_CHECKER)
    target_link_libraries(${LF_MAIN_TARGET} PUBLIC ${LIB_PREDICTED_PATH_CHECKER})
endif()
