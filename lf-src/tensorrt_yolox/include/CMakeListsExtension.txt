unset(BUILD_TESTING)
set(LF_AUTOWARE_HOME $ENV{LF_AUTOWARE_HOME})
message(STATUS "Using ${LF_AUTOWARE_HOME} as LF Autoware home directory")

include_directories(
    ${LF_AUTOWARE_HOME}/lf-src/lf-include
)

find_package(autoware_tensorrt_yolox REQUIRED)
find_package(autoware_perception_msgs REQUIRED)
find_package(autoware_utils REQUIRED)
find_package(cv_bridge REQUIRED)
find_package(image_transport REQUIRED)
find_package(rclcpp REQUIRED)
find_package(rclcpp_components REQUIRED)
find_package(sensor_msgs REQUIRED)
find_package(tier4_perception_msgs REQUIRED)

ament_target_dependencies(${LF_MAIN_TARGET} PUBLIC
    autoware_tensorrt_yolox
    autoware_perception_msgs
    autoware_utils
    cv_bridge
    image_transport
    rclcpp
    rclcpp_components
    sensor_msgs
    tier4_perception_msgs
)

find_library(LIB_TENSORRT_YOLOX autoware_tensorrt_yolox
    PATHS ${autoware_tensorrt_yolox_DIR}/../../../lib NO_DEFAULT_PATH)
if(LIB_TENSORRT_YOLOX)
    target_link_libraries(${LF_MAIN_TARGET} PUBLIC ${LIB_TENSORRT_YOLOX})
endif()
