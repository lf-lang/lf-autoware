unset(BUILD_TESTING)
set(LF_AUTOWARE_HOME $ENV{LF_AUTOWARE_HOME})
message(STATUS "Using ${LF_AUTOWARE_HOME} as LF Autoware home directory")

include_directories(
    ${LF_AUTOWARE_HOME}/lf-src/lf-include
    ${LF_AUTOWARE_HOME}/src/universe/autoware_universe/system/autoware_processing_time_checker/src
)

find_package(autoware_processing_time_checker REQUIRED)
find_package(autoware_utils REQUIRED)
find_package(rclcpp REQUIRED)
find_package(rclcpp_components REQUIRED)

ament_target_dependencies(${LF_MAIN_TARGET} PUBLIC
    autoware_processing_time_checker
    autoware_utils
    rclcpp
    rclcpp_components
)

find_library(LIB_PROCESSING_TIME_CHECKER autoware_processing_time_checker
    PATHS ${autoware_processing_time_checker_DIR}/../../../lib NO_DEFAULT_PATH)
if(LIB_PROCESSING_TIME_CHECKER)
    target_link_libraries(${LF_MAIN_TARGET} PUBLIC ${LIB_PROCESSING_TIME_CHECKER})
endif()
