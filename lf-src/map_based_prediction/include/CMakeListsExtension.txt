unset(BUILD_TESTING)
set(LF_AUTOWARE_HOME $ENV{LF_AUTOWARE_HOME})
message(STATUS "Using ${LF_AUTOWARE_HOME} as LF Autoware home directory")

include_directories(
    ${LF_AUTOWARE_HOME}/lf-src/lf-include
    ${LF_AUTOWARE_HOME}/src/universe/autoware_universe/perception/autoware_map_based_prediction/include
)

find_package(autoware_map_based_prediction REQUIRED)
find_package(autoware_internal_debug_msgs REQUIRED)
find_package(autoware_interpolation REQUIRED)
find_package(autoware_lanelet2_utils REQUIRED)
find_package(autoware_motion_utils REQUIRED)
find_package(autoware_object_recognition_utils REQUIRED)
find_package(autoware_perception_msgs REQUIRED)
find_package(autoware_utils REQUIRED)
find_package(glog REQUIRED)
find_package(rclcpp REQUIRED)
find_package(rclcpp_components REQUIRED)
find_package(tf2 REQUIRED)
find_package(tf2_geometry_msgs REQUIRED)
find_package(tf2_ros REQUIRED)
find_package(unique_identifier_msgs REQUIRED)
find_package(visualization_msgs REQUIRED)

ament_target_dependencies(${LF_MAIN_TARGET} PUBLIC
    autoware_map_based_prediction
    autoware_internal_debug_msgs
    autoware_interpolation
    autoware_lanelet2_utils
    autoware_motion_utils
    autoware_object_recognition_utils
    autoware_perception_msgs
    autoware_utils
    glog
    rclcpp
    rclcpp_components
    tf2
    tf2_geometry_msgs
    tf2_ros
    unique_identifier_msgs
    visualization_msgs
)
