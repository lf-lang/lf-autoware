unset(BUILD_TESTING)
set(LF_AUTOWARE_HOME $ENV{LF_AUTOWARE_HOME})
message(STATUS "Using ${LF_AUTOWARE_HOME} as LF Autoware home directory")

include_directories(
    ${LF_AUTOWARE_HOME}/lf-src/lf-include
)

find_package(autoware_image_projection_based_fusion REQUIRED)
find_package(autoware_euclidean_cluster REQUIRED)
find_package(autoware_object_recognition_utils REQUIRED)
find_package(autoware_perception_msgs REQUIRED)
find_package(autoware_point_types REQUIRED)
find_package(autoware_utils REQUIRED)
find_package(cv_bridge REQUIRED)
find_package(diagnostic_msgs REQUIRED)
find_package(diagnostic_updater REQUIRED)
find_package(image_geometry REQUIRED)
find_package(image_transport REQUIRED)
find_package(message_filters REQUIRED)
find_package(rclcpp REQUIRED)
find_package(rclcpp_components REQUIRED)
find_package(sensor_msgs REQUIRED)
find_package(tf2_eigen REQUIRED)
find_package(tf2_ros REQUIRED)

ament_target_dependencies(${LF_MAIN_TARGET} PUBLIC
    autoware_image_projection_based_fusion
    autoware_euclidean_cluster
    autoware_object_recognition_utils
    autoware_perception_msgs
    autoware_point_types
    autoware_utils
    cv_bridge
    diagnostic_msgs
    diagnostic_updater
    image_geometry
    image_transport
    message_filters
    rclcpp
    rclcpp_components
    sensor_msgs
    tf2_eigen
    tf2_ros
)

# Fix transitive curl dependency issue from libgdal
find_package(CURL REQUIRED)
target_link_libraries(${LF_MAIN_TARGET} PUBLIC CURL::libcurl)

# Link node implementation library
find_library(LIB_IMAGE_PROJECTION_BASED_FUSION autoware_image_projection_based_fusion
    PATHS ${autoware_image_projection_based_fusion_DIR}/../../../lib NO_DEFAULT_PATH)
if(LIB_IMAGE_PROJECTION_BASED_FUSION)
    target_link_libraries(${LF_MAIN_TARGET} PUBLIC ${LIB_IMAGE_PROJECTION_BASED_FUSION})
endif()
