unset(BUILD_TESTING)
set(LF_AUTOWARE_HOME $ENV{LF_AUTOWARE_HOME})
message(STATUS "Using ${LF_AUTOWARE_HOME} as LF Autoware home directory")

include_directories(
    ${LF_AUTOWARE_HOME}/lf-src/lf-include
    ${LF_AUTOWARE_HOME}/src/universe/autoware_universe/system/autoware_system_monitor/include
)

find_package(autoware_system_monitor REQUIRED)
find_package(diagnostic_msgs REQUIRED)
find_package(rclcpp REQUIRED)
find_package(rclcpp_components REQUIRED)
find_package(fmt REQUIRED)

# Platform-specific GPU detection
find_path(LIBNL3_INCLUDE_DIRS
    NAMES netlink/netlink.h
    PATH_SUFFIXES libnl3
)

include_directories(${LIBNL3_INCLUDE_DIRS})

ament_target_dependencies(${LF_MAIN_TARGET} PUBLIC
    autoware_system_monitor
    diagnostic_msgs
    rclcpp
    rclcpp_components
)

target_link_libraries(${LF_MAIN_TARGET} PUBLIC fmt::fmt)
