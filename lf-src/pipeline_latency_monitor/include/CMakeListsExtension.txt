unset(BUILD_TESTING)
set(LF_AUTOWARE_HOME $ENV{LF_AUTOWARE_HOME})
message(STATUS "Using ${LF_AUTOWARE_HOME} as LF Autoware home directory")

# Fix tinyxml2_vendor issue: pre-set TinyXML2 vars so vendor creates correct target
set(TinyXML2_FOUND TRUE)
set(TINYXML2_LIBRARY "/usr/lib/x86_64-linux-gnu/libtinyxml2.so")
set(TINYXML2_INCLUDE_DIR "/usr/include")


include_directories(
    ${LF_AUTOWARE_HOME}/lf-src/lf-include
    ${LF_AUTOWARE_HOME}/src/universe/autoware_universe/system/autoware_pipeline_latency_monitor/src
)

find_package(autoware_pipeline_latency_monitor REQUIRED)
find_package(autoware_control_msgs REQUIRED)
find_package(autoware_planning_msgs REQUIRED)
find_package(diagnostic_msgs REQUIRED)
find_package(diagnostic_updater REQUIRED)
find_package(rclcpp REQUIRED)
find_package(rclcpp_components REQUIRED)

ament_target_dependencies(${LF_MAIN_TARGET} PUBLIC
    autoware_pipeline_latency_monitor
    autoware_control_msgs
    autoware_planning_msgs
    diagnostic_msgs
    diagnostic_updater
    rclcpp
    rclcpp_components
)

find_library(LIB_PIPELINE_LATENCY_MONITOR autoware_pipeline_latency_monitor
    PATHS ${autoware_pipeline_latency_monitor_DIR}/../../../lib NO_DEFAULT_PATH)
if(LIB_PIPELINE_LATENCY_MONITOR)
    target_link_libraries(${LF_MAIN_TARGET} PUBLIC ${LIB_PIPELINE_LATENCY_MONITOR})
endif()
