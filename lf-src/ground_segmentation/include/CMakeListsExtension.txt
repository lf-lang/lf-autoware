unset(BUILD_TESTING)
set(LF_AUTOWARE_HOME $ENV{LF_AUTOWARE_HOME})
message(STATUS "Using ${LF_AUTOWARE_HOME} as LF Autoware home directory")

# Fix tinyxml2_vendor issue: pre-set TinyXML2 vars so vendor creates correct target
set(TinyXML2_FOUND TRUE)
set(TINYXML2_LIBRARY "/usr/lib/x86_64-linux-gnu/libtinyxml2.so")
set(TINYXML2_INCLUDE_DIR "/usr/include")

include_directories(
    ${LF_AUTOWARE_HOME}/lf-src/lf-include
    ${LF_AUTOWARE_HOME}/src/universe/autoware_universe/perception/autoware_ground_segmentation/src/scan_ground_filter
)

find_package(autoware_ground_segmentation REQUIRED)
find_package(autoware_pointcloud_preprocessor REQUIRED)
find_package(autoware_utils REQUIRED)
find_package(autoware_vehicle_info_utils REQUIRED)
find_package(pcl_conversions REQUIRED)
find_package(pcl_ros REQUIRED)
find_package(sensor_msgs REQUIRED)
find_package(std_msgs REQUIRED)
find_package(rclcpp REQUIRED)
find_package(rclcpp_components REQUIRED)
find_package(tf2 REQUIRED)
find_package(tf2_eigen REQUIRED)
find_package(tf2_ros REQUIRED)

ament_target_dependencies(${LF_MAIN_TARGET} PUBLIC
    autoware_ground_segmentation
    autoware_pointcloud_preprocessor
    autoware_utils
    autoware_vehicle_info_utils
    pcl_conversions
    pcl_ros
    sensor_msgs
    std_msgs
    rclcpp
    rclcpp_components
    tf2
    tf2_eigen
    tf2_ros
)

# Link node implementation library
find_library(LIB_GROUND_SEGMENTATION autoware_ground_segmentation
    PATHS ${autoware_ground_segmentation_DIR}/../../../lib NO_DEFAULT_PATH)
if(LIB_GROUND_SEGMENTATION)
    target_link_libraries(${LF_MAIN_TARGET} PUBLIC ${LIB_GROUND_SEGMENTATION})
endif()
