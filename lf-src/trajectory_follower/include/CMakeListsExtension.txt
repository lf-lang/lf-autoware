unset(BUILD_TESTING)
set(LF_AUTOWARE_HOME $ENV{LF_AUTOWARE_HOME})
message(STATUS "Using ${LF_AUTOWARE_HOME} as LF Autoware home directory")

include_directories(
    ${LF_AUTOWARE_HOME}/lf-src/lf-include
)

find_package(autoware_trajectory_follower_node REQUIRED)
find_package(autoware_control_msgs REQUIRED)
find_package(autoware_planning_msgs REQUIRED)
find_package(autoware_vehicle_msgs REQUIRED)
find_package(autoware_vehicle_info_utils REQUIRED)
find_package(autoware_mpc_lateral_controller REQUIRED)
find_package(autoware_pid_longitudinal_controller REQUIRED)
find_package(autoware_trajectory_follower_base REQUIRED)
find_package(rclcpp REQUIRED)
find_package(rclcpp_components REQUIRED)
find_package(tf2_ros REQUIRED)

include_directories(
    ${LF_AUTOWARE_HOME}/src/universe/autoware_universe/control/autoware_trajectory_follower_node/include
)

ament_target_dependencies(${LF_MAIN_TARGET} PUBLIC
    autoware_trajectory_follower_node
    autoware_control_msgs
    autoware_planning_msgs
    autoware_vehicle_msgs
    autoware_vehicle_info_utils
    autoware_mpc_lateral_controller
    autoware_pid_longitudinal_controller
    autoware_trajectory_follower_base
    rclcpp
    rclcpp_components
    tf2_ros
)
