unset(BUILD_TESTING)
set(LF_AUTOWARE_HOME $ENV{LF_AUTOWARE_HOME})
message(STATUS "Using ${LF_AUTOWARE_HOME} as LF Autoware home directory")

include_directories(
    ${LF_AUTOWARE_HOME}/lf-src/lf-include
)

find_package(autoware_obstacle_collision_checker REQUIRED)
find_package(autoware_planning_msgs REQUIRED)
find_package(autoware_utils REQUIRED)
find_package(autoware_vehicle_info_utils REQUIRED)
find_package(diagnostic_updater REQUIRED)
find_package(geometry_msgs REQUIRED)
find_package(nav_msgs REQUIRED)
find_package(pcl_ros REQUIRED)
find_package(rclcpp REQUIRED)
find_package(rclcpp_components REQUIRED)
find_package(sensor_msgs REQUIRED)
find_package(tf2 REQUIRED)
find_package(tf2_eigen REQUIRED)
find_package(tf2_geometry_msgs REQUIRED)
find_package(tf2_ros REQUIRED)

ament_target_dependencies(${LF_MAIN_TARGET} PUBLIC
    autoware_obstacle_collision_checker
    autoware_planning_msgs
    autoware_utils
    autoware_vehicle_info_utils
    diagnostic_updater
    geometry_msgs
    nav_msgs
    pcl_ros
    rclcpp
    rclcpp_components
    sensor_msgs
    tf2
    tf2_eigen
    tf2_geometry_msgs
    tf2_ros
)

find_library(LIB_OBSTACLE_COLLISION_CHECKER autoware_obstacle_collision_checker
    PATHS ${autoware_obstacle_collision_checker_DIR}/../../../lib NO_DEFAULT_PATH)
if(LIB_OBSTACLE_COLLISION_CHECKER)
    target_link_libraries(${LF_MAIN_TARGET} PUBLIC ${LIB_OBSTACLE_COLLISION_CHECKER})
endif()
