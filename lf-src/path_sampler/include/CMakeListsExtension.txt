unset(BUILD_TESTING)
set(LF_AUTOWARE_HOME $ENV{LF_AUTOWARE_HOME})
message(STATUS "Using ${LF_AUTOWARE_HOME} as LF Autoware home directory")

include_directories(
    ${LF_AUTOWARE_HOME}/lf-src/lf-include
)

find_package(autoware_path_sampler REQUIRED)
find_package(autoware_interpolation REQUIRED)
find_package(autoware_motion_utils REQUIRED)
find_package(autoware_perception_msgs REQUIRED)
find_package(autoware_planning_msgs REQUIRED)
find_package(autoware_utils REQUIRED)
find_package(autoware_vehicle_info_utils REQUIRED)
find_package(geometry_msgs REQUIRED)
find_package(nav_msgs REQUIRED)
find_package(rclcpp REQUIRED)
find_package(rclcpp_components REQUIRED)
find_package(std_msgs REQUIRED)
find_package(tf2_ros REQUIRED)
find_package(tier4_planning_msgs REQUIRED)
find_package(visualization_msgs REQUIRED)

ament_target_dependencies(${LF_MAIN_TARGET} PUBLIC
    autoware_path_sampler
    autoware_interpolation
    autoware_motion_utils
    autoware_perception_msgs
    autoware_planning_msgs
    autoware_utils
    autoware_vehicle_info_utils
    geometry_msgs
    nav_msgs
    rclcpp
    rclcpp_components
    std_msgs
    tf2_ros
    tier4_planning_msgs
    visualization_msgs
)

find_library(LIB_PATH_SAMPLER autoware_path_sampler
    PATHS ${autoware_path_sampler_DIR}/../../../lib NO_DEFAULT_PATH)
if(LIB_PATH_SAMPLER)
    target_link_libraries(${LF_MAIN_TARGET} PUBLIC ${LIB_PATH_SAMPLER})
endif()
